{
  "name": "cascade",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Autonomous trading agent — trades the ripple, not the splash.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  }
}
